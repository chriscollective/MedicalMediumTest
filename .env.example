# ================================
# 環境變數範例文件
# ================================
#
# 使用說明：
# 1. 複製此文件為 .env
# 2. 填入實際的值（不要使用範例值）
# 3. 確保 .env 已加入 .gitignore（不要上傳到 Git）
#

# ================================
# 資料庫設定
# ================================

# MongoDB Atlas 連線字串
# 取得方式：MongoDB Atlas > Connect > Drivers
# 格式：mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-database?retryWrites=true&w=majority

# ================================
# JWT 認證設定
# ================================

# JWT 加密金鑰（至少 32 字元）
# 🚨 重要：請使用強隨機字串，不要使用此範例值！
#
# 生成方式：
# Node.js: require('crypto').randomBytes(64).toString('hex')
# OpenSSL: openssl rand -base64 64
# 線上工具: https://www.random.org/strings/
#
JWT_SECRET=PLEASE-CHANGE-THIS-TO-A-VERY-LONG-RANDOM-STRING-AT-LEAST-32-CHARACTERS

# JWT Token 有效期限（預設 7 天）
# JWT_EXPIRES_IN=7d

# ================================
# 伺服器設定
# ================================

# 後端伺服器端口
PORT=5000

# 執行環境（development / production）
NODE_ENV=development

# ================================
# 前端設定（Vite）
# ================================

# 前端 API 位址
# 開發環境使用 localhost:5000
# 生產環境改為實際 API 網址
VITE_API_URL=http://localhost:5000/api

# ================================
# 可選設定
# ================================

# 前端網址（用於 CORS 白名單）
# FRONTEND_URL=https://your-frontend-domain.com

# 日誌等級（error / warn / info / debug）
# LOG_LEVEL=info

# ================================
# 資安建議
# ================================
#
# ✅ DO：
# - 使用強隨機字串（至少 32 字元）
# - 定期更換 JWT_SECRET（重新登入所有使用者）
# - 不同環境使用不同的 SECRET
# - 將 .env 加入 .gitignore
#
# ❌ DON'T：
# - 不要使用簡單的密碼或字串
# - 不要將 .env 上傳到 Git
# - 不要在程式碼中硬編碼 SECRET
# - 不要使用預設值或範例值
#
